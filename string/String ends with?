public class Kata {
    public static boolean solution(String str, String ending) {
      if(ending.length()>str.length()){
        return false;
      }
      String last = str.substring(str.length()-ending.length());
      return last.equals(ending);
    }
}
